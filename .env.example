# Leave VITE_SENTRY_DSN empty in development to keep Sentry completely off.
# Set it only in your production deployment environment — never commit a real DSN here.
VITE_API_URL=http://localhost:8000
VITE_SENTRY_DSN=
VITE_SENTRY_ENV=
VITE_APP_VERSION=
